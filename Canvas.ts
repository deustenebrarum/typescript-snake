namespace SnakeProject {
    export class Canvas {
        readonly width: number;
        readonly height: number;

        constructor(width: number, height: number) {
            this.width = width;
            this.height = height;
        }

        public deleteObject(point: Point, radius: number) {
            
        }

        public createObject(point: Point, radius: number) {

        }
    }
}